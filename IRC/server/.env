# Server configuration for Render deployment
IRC_SERVER_HOST=0.0.0.0
IRC_SERVER_PORT=443
IRC_STATE_PATH=data/irc-ultra-state.json
IRC_SERVER_URL=https://abyss-irc-server.onrender.com/

# For Render, we need to use the PORT environment variable
# The PORT variable is set by Render automatically
PORT=443

# Enable HTTPS for Render deployment
NODE_ENV=production